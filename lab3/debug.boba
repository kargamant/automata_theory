big calcPow normal a normal n begin check n<=0 do return 1,. check n<=1 do return a,. big res<<1, until n<=0 do res<<(res*a), n<<(n-1),. return res,. end,
big bin normal b begin normal div<<b/2, tiny digit<<0, big res power<<0, until b<=0 do div<<b/2, digit<<(b-2*div), res<<(res+digit*calcPow(10 power)), power<<(power+1), b<<(b/2),. return res,. end,
@-2000,
@sonar,
@-2000,
big GG<<bin(sonar),
@GG,
.
